# build
dist/
.astro/
.netlify/

# deps
node_modules/

# secrets — never commit
.env
.env.production
.env.local

# PDF de la documentacion: los originales que se van a proteger y las
# copias ya encriptadas. Ninguno de los dos entra al repositorio.
documentos-a-proteger/
documentos-protegidos/

# Restricted documentation. These files are served only through the
# authenticated endpoint; they must never end up in the public build.
documentos/*
!documentos/README.md

# os
.DS_Store
Thumbs.db

# Página de Drive guardada por error al buscar el logo
logo tassaroli.html
logo tassaroli_files/

# Renders originales sin optimizar: pesados, y el sitio usa los de src/assets
renders-originales/

# Master del video institucional: 752 MB en 4K a 33 Mbps, tal como lo
# entregó el cliente. Al sitio va la versión comprimida en public/video/;
# el master se guarda aparte y no entra al repositorio.
/C. F. TSA .mp4
*.master.mp4

# Artefactos del skill de diseño
.impeccable/

# Cache de Python
__pycache__/
*.pyc
